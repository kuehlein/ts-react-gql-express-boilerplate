import React, { SFC } from "react";
import { hot } from "react-hot-loader";

/**
 * Landing page...
 */
const Landing: SFC<{}> = () => (
  <div>
    <h2>Landing page</h2>
  </div>
);

export default hot(module)(Landing);
